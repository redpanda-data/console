# Misc
.DS_Store
notes.md
fixname.sh

# IDEs
**/.vscode
**/.idea
**/*.code-workspace

# Helper Scripts
run-docker.sh
run-backend.sh
requests.txt

# Local Formatting
.prettierrc

# Local build tools installed via Taskfiles
build

.cursor
# Showcase PNGs are hosted on orphan branch pr-screenshots-2389 rather
# than committed to feature branches — keeps binaries out of the main
# merge history. Regenerate via `bun run test:browser:update` when a
# component changes.
frontend/docs/pr-screenshots/
