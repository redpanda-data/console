{
  "_comment": "Merged coverage thresholds for the frontend test suite. Baseline was captured with `bun run test:coverage` on branch frontend/upgrade-vitest-4.1.4 (2026-04-16). Values are baseline - 1% so the gate only catches regressions, not natural test-run noise. Update this file when the real baseline rises materially; do NOT set aspirational targets.",
  "thresholds": {
    "lines": 28.73,
    "statements": 28.2,
    "functions": 23.35,
    "branches": 22.76
  }
}
