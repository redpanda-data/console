[install]
minimumReleaseAge = 259200 # 72h supply-chain publish delay
# BSR-generated packages from Redpanda's own protos; exempt from the publish
# delay so proto updates can be consumed as soon as they are published
minimumReleaseAgeExcludes = [
  "@buf/redpandadata_cloud.connectrpc_query-es",
  "@buf/redpandadata_cloud.bufbuild_es",
  "@buf/redpandadata_core.bufbuild_es",
  "@buf/redpandadata_common.bufbuild_es",
]
save = true
saveTextLockfile = true

# print must live under [install.lockfile]; under [install] it is ignored
# and the yarn.lock Snyk mirror silently goes stale
[install.lockfile]
print = "yarn"
