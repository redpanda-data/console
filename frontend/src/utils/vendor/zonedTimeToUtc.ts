// Build-time stub for the v2 deep path `date-fns-tz/zonedTimeToUtc`. See
// `./date-fns-tz-shim.ts` for context.
const unreachable = () => {
  throw new Error('date-fns-tz/zonedTimeToUtc shim: unreachable — DateTimeInput was replaced.');
};

export default unreachable;
