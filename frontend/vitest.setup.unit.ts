// Minimal setup for pure unit tests
// No DOM mocking, no React-specific mocks
// These tests run in Node environment and don't need browser APIs

// Export empty object to satisfy noEmptySource rule
export {};
