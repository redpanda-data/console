// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: redpanda/api/aigateway/v1/gateway_config.proto

package aigatewayv1connect

import (
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"

	connect "connectrpc.com/connect"
	v1 "go.panda.dev/redpanda-aigw/protos/gen/redpanda/api/aigateway/v1"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GatewayConfigServiceName is the fully-qualified name of the GatewayConfigService service.
	GatewayConfigServiceName = "redpanda.api.aigateway.v1.GatewayConfigService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GatewayConfigServiceStreamConfigProcedure is the fully-qualified name of the
	// GatewayConfigService's StreamConfig RPC.
	GatewayConfigServiceStreamConfigProcedure = "/redpanda.api.aigateway.v1.GatewayConfigService/StreamConfig"
	// GatewayConfigServiceFetchConfigProcedure is the fully-qualified name of the
	// GatewayConfigService's FetchConfig RPC.
	GatewayConfigServiceFetchConfigProcedure = "/redpanda.api.aigateway.v1.GatewayConfigService/FetchConfig"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	gatewayConfigServiceServiceDescriptor            = v1.File_redpanda_api_aigateway_v1_gateway_config_proto.Services().ByName("GatewayConfigService")
	gatewayConfigServiceStreamConfigMethodDescriptor = gatewayConfigServiceServiceDescriptor.Methods().ByName("StreamConfig")
	gatewayConfigServiceFetchConfigMethodDescriptor  = gatewayConfigServiceServiceDescriptor.Methods().ByName("FetchConfig")
)

// GatewayConfigServiceClient is a client for the redpanda.api.aigateway.v1.GatewayConfigService
// service.
type GatewayConfigServiceClient interface {
	// StreamConfig establishes a bidirectional stream for configuration updates.
	//
	// Flow:
	//  1. Gateway sends StreamConfigRequest with node info and optional last_version
	//  2. Control plane sends initial StreamConfigResponse with full snapshot (if no version)
	//     or delta updates since last_version
	//  3. Gateway sends ACK with received version
	//  4. Control plane streams delta updates as resources change
	//  5. Gateway ACKs each update
	//
	// On reconnection, gateway sends last successfully applied version to resume
	// from where it left off.
	StreamConfig(context.Context) *connect.BidiStreamForClient[v1.StreamConfigRequest, v1.StreamConfigResponse]
	// FetchConfig retrieves the current configuration snapshot.
	// Use this for simple polling-based updates or initial bootstrap.
	// For real-time updates, prefer StreamConfig.
	FetchConfig(context.Context, *connect.Request[v1.FetchConfigRequest]) (*connect.Response[v1.FetchConfigResponse], error)
}

// NewGatewayConfigServiceClient constructs a client for the
// redpanda.api.aigateway.v1.GatewayConfigService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGatewayConfigServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GatewayConfigServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &gatewayConfigServiceClient{
		streamConfig: connect.NewClient[v1.StreamConfigRequest, v1.StreamConfigResponse](
			httpClient,
			baseURL+GatewayConfigServiceStreamConfigProcedure,
			connect.WithSchema(gatewayConfigServiceStreamConfigMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		fetchConfig: connect.NewClient[v1.FetchConfigRequest, v1.FetchConfigResponse](
			httpClient,
			baseURL+GatewayConfigServiceFetchConfigProcedure,
			connect.WithSchema(gatewayConfigServiceFetchConfigMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// gatewayConfigServiceClient implements GatewayConfigServiceClient.
type gatewayConfigServiceClient struct {
	streamConfig *connect.Client[v1.StreamConfigRequest, v1.StreamConfigResponse]
	fetchConfig  *connect.Client[v1.FetchConfigRequest, v1.FetchConfigResponse]
}

// StreamConfig calls redpanda.api.aigateway.v1.GatewayConfigService.StreamConfig.
func (c *gatewayConfigServiceClient) StreamConfig(ctx context.Context) *connect.BidiStreamForClient[v1.StreamConfigRequest, v1.StreamConfigResponse] {
	return c.streamConfig.CallBidiStream(ctx)
}

// FetchConfig calls redpanda.api.aigateway.v1.GatewayConfigService.FetchConfig.
func (c *gatewayConfigServiceClient) FetchConfig(ctx context.Context, req *connect.Request[v1.FetchConfigRequest]) (*connect.Response[v1.FetchConfigResponse], error) {
	return c.fetchConfig.CallUnary(ctx, req)
}

// GatewayConfigServiceHandler is an implementation of the
// redpanda.api.aigateway.v1.GatewayConfigService service.
type GatewayConfigServiceHandler interface {
	// StreamConfig establishes a bidirectional stream for configuration updates.
	//
	// Flow:
	//  1. Gateway sends StreamConfigRequest with node info and optional last_version
	//  2. Control plane sends initial StreamConfigResponse with full snapshot (if no version)
	//     or delta updates since last_version
	//  3. Gateway sends ACK with received version
	//  4. Control plane streams delta updates as resources change
	//  5. Gateway ACKs each update
	//
	// On reconnection, gateway sends last successfully applied version to resume
	// from where it left off.
	StreamConfig(context.Context, *connect.BidiStream[v1.StreamConfigRequest, v1.StreamConfigResponse]) error
	// FetchConfig retrieves the current configuration snapshot.
	// Use this for simple polling-based updates or initial bootstrap.
	// For real-time updates, prefer StreamConfig.
	FetchConfig(context.Context, *connect.Request[v1.FetchConfigRequest]) (*connect.Response[v1.FetchConfigResponse], error)
}

// NewGatewayConfigServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGatewayConfigServiceHandler(svc GatewayConfigServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gatewayConfigServiceStreamConfigHandler := connect.NewBidiStreamHandler(
		GatewayConfigServiceStreamConfigProcedure,
		svc.StreamConfig,
		connect.WithSchema(gatewayConfigServiceStreamConfigMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	gatewayConfigServiceFetchConfigHandler := connect.NewUnaryHandler(
		GatewayConfigServiceFetchConfigProcedure,
		svc.FetchConfig,
		connect.WithSchema(gatewayConfigServiceFetchConfigMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/redpanda.api.aigateway.v1.GatewayConfigService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GatewayConfigServiceStreamConfigProcedure:
			gatewayConfigServiceStreamConfigHandler.ServeHTTP(w, r)
		case GatewayConfigServiceFetchConfigProcedure:
			gatewayConfigServiceFetchConfigHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGatewayConfigServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGatewayConfigServiceHandler struct{}

func (UnimplementedGatewayConfigServiceHandler) StreamConfig(context.Context, *connect.BidiStream[v1.StreamConfigRequest, v1.StreamConfigResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("redpanda.api.aigateway.v1.GatewayConfigService.StreamConfig is not implemented"))
}

func (UnimplementedGatewayConfigServiceHandler) FetchConfig(context.Context, *connect.Request[v1.FetchConfigRequest]) (*connect.Response[v1.FetchConfigResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("redpanda.api.aigateway.v1.GatewayConfigService.FetchConfig is not implemented"))
}
